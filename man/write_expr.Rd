% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/write-expr.R
\name{write_expr}
\alias{write_expr}
\title{Build expression to write data to file(s)}
\usage{
write_expr(
  data_names,
  directory,
  filename = "",
  format = c("csv", "excel", "parquet", "feather"),
  args = list()
)
}
\arguments{
\item{data_names}{Character vector of data object names.
Names of the vector (if present) are used for sheet/file naming.}

\item{directory}{Character. Directory path for output}

\item{filename}{Character. Base filename (without extension).
If empty, generates timestamped name.}

\item{format}{Character. File format: "csv", "excel", "parquet", "feather"}

\item{args}{List of format-specific write parameters}
}
\value{
A language object (expression) that writes file(s)
}
\description{
Main dispatcher that generates appropriate write expression based on
format and number of data objects.
}
\examples{
\dontrun{
# Single CSV
expr <- write_expr(c(data1 = "df"), "/tmp", "output", "csv", list(sep = ";"))
eval(expr)

# Multiple sheets in Excel
expr <- write_expr(c(sales = "df1", costs = "df2"), "/tmp", "report", "excel")
eval(expr)
}

}
\keyword{internal}
