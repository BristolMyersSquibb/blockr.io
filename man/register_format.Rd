% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{register_format}
\alias{register_format}
\title{Register a file format}
\usage{
register_format(extensions, read)
}
\arguments{
\item{extensions}{Character vector of file extensions (without dots).}

\item{read}{\verb{function(path, ...)} returning an unevaluated expression that
reads \code{path} into a data frame. The expression may only call the reader
package (e.g. \code{bquote(your.pkg::read_special(.(path)))}), never blockr.
\code{...} carries format options forwarded by the calling block. \code{path} is a
resolved location and may be a language object (a container splicing
member reads against a run-time prefix), so splice it, do not inspect it.}
}
\value{
Invisible \code{NULL}, called for its side effect.
}
\description{
Teaches blockr.io a file format at load time. The format then works
everywhere blockr.io's format knowledge is consulted: the read block's
dispatch, \code{\link[=file_extensions]{file_extensions()}} (and with it the file browser filter and
upload accept lists), and any package that asks the registry rather than
carrying its own extension list.
}
\details{
Call from your package's \code{.onLoad()}. The registering namespace is
recorded automatically for collision messages. Registration never throws;
two packages claiming the same extension and mode error at first lookup,
naming both.
}
\seealso{
\code{\link[=register_container]{register_container()}}, \code{\link[=format_read_expr]{format_read_expr()}}
}
