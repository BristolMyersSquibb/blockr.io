% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/path-input.R
\name{list_dir_response}
\alias{list_dir_response}
\title{Directory-listing response for path autocomplete}
\usage{
list_dir_response(
  path_val,
  dir_root = "",
  mode = "file",
  extensions = NULL,
  policy = "read"
)
}
\arguments{
\item{path_val}{The raw text typed in the input.}

\item{dir_root}{Optional data directory against which relative input is
resolved. Empty string means no data directory.}

\item{mode}{\code{"file"} lists directories plus files with allowed extensions;
\code{"directory"} lists directories only.}

\item{extensions}{Optional file extension allowlist (without dots) for
file mode; \code{NULL} means all rio-supported formats.}

\item{policy}{\code{"read"} or \code{"write"}, selecting which deployment verifier
gates the listing.}
}
\value{
A \code{\link[shiny:httpResponse]{shiny::httpResponse()}} with JSON body
\verb{\{items, base, total\}}.
}
\description{
Shared implementation behind the \code{registerDataObj} endpoints of
\code{\link[=path_input_server]{path_input_server()}} and \code{\link[=new_data_dir_option]{new_data_dir_option()}}. Resolves the typed
value to a directory to list, applies the deployment file-access policy
(see \link{file_policy}) to that directory, and returns a JSON \code{httpResponse}
with up to 50 entries plus the total match count.
}
\details{
Entries are ranked directories first, then prefix matches on the typed
fragment before substring matches, then alphabetically. All entries of a
directory are stat-ed with a single vectorized \code{\link[=file.info]{file.info()}} call.
}
\keyword{internal}
