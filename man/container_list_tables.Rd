% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{container_list_tables}
\alias{container_list_tables}
\alias{container_table_info}
\title{Discover the tables in a container}
\usage{
container_list_tables(path)

container_table_info(path)
}
\arguments{
\item{path}{Character. A resolved location.}
}
\value{
For \code{container_list_tables()}, a character vector of table names.
For \code{container_table_info()}, a data frame with columns \code{name} and
\code{label}.
}
\description{
Resolves \code{path} (a directory, or a file whose extension has a container
entry) and returns the table names its entry can see, without reading
table data where the format allows. \code{\link[=container_table_info]{container_table_info()}} returns the
same discovery with each table's display label (\code{"CSV 2.1 MB"},
\code{"12 rows"}, \code{""} when the entry has none), for blocks that render a
picker.
}
