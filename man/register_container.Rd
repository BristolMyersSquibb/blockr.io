% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/registry.R
\name{register_container}
\alias{register_container}
\title{Register a container}
\usage{
register_container(opens, list_tables, read)
}
\arguments{
\item{opens}{Character vector of file extensions (without dots), or the
string \code{"directory"}. Directories are not a special case in the lookup,
only a key that is not an extension.}

\item{list_tables}{\verb{function(path)} returning the table names available at
\code{path}, cheaply: discovery must not read table data where the format
allows it (a zip's central directory, a workbook's sheet index). Formats
whose only index is the data itself (an \code{.rds}) may read; they are local
files read once at configure time. Return a character vector, or a data
frame with a \code{name} column and an optional \code{label} column carrying a
short display string per table (\code{"CSV 2.1 MB"}).}

\item{read}{\verb{function(path, tables, ...)} returning one unevaluated
expression that evaluates to a named list of data frames, restricted to
\code{tables} (\code{NULL} means all). The expression may compose member reads via
\code{\link[=format_read_expr]{format_read_expr()}} at build time, and may only call reader packages.}
}
\value{
Invisible \code{NULL}, called for its side effect.
}
\description{
A container is a location that holds several tables: a directory, a zip,
a workbook read sheet by sheet. A container entry produces a bare named
list of data frames -- no class. Whatever the consuming block wants the
list to become (a dm, via \code{dm::new_dm()}) is its own wrap, made outside
this package.
}
\seealso{
\code{\link[=register_format]{register_format()}}, \code{\link[=container_list_tables]{container_list_tables()}},
\code{\link[=container_read_expr]{container_read_expr()}}
}
