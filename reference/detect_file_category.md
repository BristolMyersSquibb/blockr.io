# Detect file category for UI adaptation

Categorizes files by extension into broad categories that determine
which UI options to show (csv/excel/arrow/other).

## Usage

``` r
detect_file_category(path)
```

## Arguments

- path:

  Character. File path.

## Value

Character. One of: "csv", "excel", "arrow", "other"
