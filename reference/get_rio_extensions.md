# Get list of file extensions supported by rio

Returns a comprehensive list of file formats that can be handled by
rio::import(). Used for file browser accept parameter and format
validation.

## Usage

``` r
get_rio_extensions()
```

## Value

Character vector of file extensions (without dots)
